<Page
    x:Class="GraphicsProject.Views.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Coding4Fun.Toolkit.Controls"
    mc:Ignorable="d">

    <SplitView x:Name="MySplitView" DisplayMode="CompactOverlay" IsPaneOpen="False" 
               CompactPaneLength="50" OpenPaneLength="300">
        <SplitView.Pane>
            <StackPanel Background="Beige">
                <Button x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;"
                        Width="50" Height="50" Background="Transparent" Click="HamburgerButton_Click"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="NewImgButton" FontFamily="Segoe MDL2 Assets" Content="&#xE1DA;"
                            Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="Nowy" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="OpenImgButton" FontFamily="Segoe MDL2 Assets" Content="&#xE197;"
                            Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="Otwórz" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="SaveImgButton" FontFamily="Segoe MDL2 Assets" Content="&#xE105;"
                            Width="50" Height="50" Background="Transparent"/>
                    <TextBlock Text="Zapisz" FontSize="18" VerticalAlignment="Center" />
                </StackPanel>
            </StackPanel>
        </SplitView.Pane>
        <SplitView.Content>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="7*"/>
                </Grid.RowDefinitions>
                
                <Grid Row="1" Background="Gray">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="7*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Canvas Grid.Column="0" Name="DrawingCanvas" Background="White"
                            PointerPressed="Canvas_OnPointerPressed" 
                            PointerReleased="Canvas_OnPointerReleased" 
                            PointerMoved="Canvas_OnPointerMoved">
                        <!--<Line Name="MyEllipse" ManipulationMode="TranslateX, TranslateY, Scale" X1="0" X2="100" Y1="0" Y2="100" Stroke="Black"
                                 Fill="Aqua" Width="100" Height="100" Canvas.Left="844" Canvas.Top="232" />-->
                    </Canvas>
                    <Grid Grid.Column="1" Background="Silver">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="3*"></RowDefinition>
                            <RowDefinition Height="3*"></RowDefinition>
                            <RowDefinition Height="5*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0"></Grid>
                        <Grid Grid.Row="2">
                            <controls:ColorPicker x:Name="Colorpick"  ColorChanged="Colorpick_ColorChanged" Visibility="Visible" />
                        </Grid>
                    </Grid>
                </Grid>
                <Grid Row="0" Background="Black"/>
            </Grid>
        </SplitView.Content>
    </SplitView>
</Page>
